[phases.setup]
nixPkgs = ['stockfish']
aptPkgs = ['stockfish']

[phases.build]
cmds = ['mvn clean package -DskipTests']

[start]
cmd = 'java -jar target/blind-chess-1.0.0.jar'

[variables]
STOCKFISH_PATH = '/usr/games/stockfish'
